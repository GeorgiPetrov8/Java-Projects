package com.company;  import java.util.Scanner;  public class Main {      public static void main(String[] args) {         Scanner scanner = new Scanner(System.in);         double budget = Double.parseDouble(scanner.nextLine());         double spendMoney = 0;         String input = scanner.nextLine();         int flowers= 0;         int balloons= 0;         int candles = 0;         int ribbon = 0;         boolean a = true;         while(a){             if(input.equals("stop")){                 System.out.printf("Spend money: %.2f\n" +                         "Money left: %.2f\n" +                         "Purchased decoration is %d balloons, %d m ribbon, %d flowers and %d candles.",spendMoney,budget,balloons,ribbon,flowers,candles);                 break;             }             String productType = input;             int numberOfProduct = Integer.parseInt(scanner.nextLine());             double priceForOneProduct = 0;              if(productType.equals("flowers")){                 priceForOneProduct = 1.5;                 double price = priceForOneProduct * numberOfProduct;                     budget -= price;                     spendMoney += price;                     flowers += numberOfProduct;              }else if (productType.equals("balloons")){                 priceForOneProduct = 0.1;                 double price = priceForOneProduct * numberOfProduct;                     budget -= price;                 spendMoney += price;                 balloons += numberOfProduct;              }else if (productType.equals("candles")){                 priceForOneProduct = 0.5;                 double price = priceForOneProduct * numberOfProduct;                     budget -= price;                 spendMoney += price;                 candles += numberOfProduct;              }else if(productType.equals("ribbon")){                 priceForOneProduct = 2;                 double price = priceForOneProduct * numberOfProduct;                     budget -= price;                 spendMoney += price;                 ribbon += numberOfProduct;             }               if(budget <= 0){                 System.out.printf("All money is spent!\n" +                         "Purchased decoration is %d balloons, %d m ribbon, %d flowers and %d candles.",balloons,ribbon,flowers,candles);                 break;             }             input = scanner.nextLine();         }         System.out.println();     } } 


